# Runtime dependencies for the BiasTracker Streamlit app on hosts that install
# from pip (e.g. Streamlit Community Cloud). This file lives next to app.py so
# the host uses pip here instead of running Poetry on pyproject.toml.
#
# The two in-repo packages (biastracker, protperties) are imported from source
# via a sys.path shim at the top of app.py, so only their third-party
# dependencies are listed below.
#
# IMPORTANT: the native scientific stack is PINNED to a mature, mutually
# compatible set with well-tested manylinux wheels. Leaving these unpinned lets
# the Cloud installer resolve bleeding-edge Linux builds (e.g. pandas 3.0 +
# newest pyarrow/numpy/scipy) whose binaries segfault the Streamlit server on
# Linux even though they work locally on Windows. Bump these deliberately, and
# only after confirming the new set boots on Streamlit Cloud.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
statsmodels==0.14.4
pyarrow==17.0.0
matplotlib==3.9.2
biopython==1.84

# Pure-Python (or non-segfaulting) deps — kept as flexible ranges.
streamlit>=1.28,<2
plotly>=5.0
requests>=2.32
typer>=0.9
pydantic
pyyaml
pyteomics>=4.6
openpyxl>=3.0
